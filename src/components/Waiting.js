import React, { PropTypes } from 'react';

const Waiting = React.createClass({
  render () {
    return (
      <h3>You are waiting for someone else to join the game</h3>
    );
  }
});

export default Waiting;
