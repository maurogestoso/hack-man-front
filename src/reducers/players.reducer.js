const initialState = [
  {
    name: 'Player 1',
    position: {
      row: 0,
      col: 0
    }
  }
];

const board = (prevState = initialState, action) => {
  return prevState;
};

export default board;